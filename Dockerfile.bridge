FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY bridge_server.py .

ENV BRIDGE_BIND_HOST=0.0.0.0
ENV BRIDGE_BIND_PORT=8765

EXPOSE 8765

CMD ["python", "bridge_server.py", "--host", "0.0.0.0", "--port", "8765"]
